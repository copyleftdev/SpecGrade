openapi: 3.1.0
components:
  schemas:
    VariantId:
      type: string
    Variant:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/VariantId'
        name:
          type: string
          minLength: 1
